on:
  push:
    branches:
      - jabirlegacyauto
name: 🚀 Deploy Application on push jabirlegacyauto
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ftp.cloud209.cloudwebhosting.com
        username: ${{ secrets.JABIRLEG_USER }}
        password:  ${{ secrets.JABRIL_PASSWORD }}
        server-dir: ./
