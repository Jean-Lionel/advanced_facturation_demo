models:
  PeriodePaimentLocation:
    user_id: id foreign:users
    year: string nullable
    month: string nullable
    status: string nullable
    SoftDeletes



seeders: PeriodePaimentLocation

controllers:
  PeriodePaimentLocation:
    resource: web

