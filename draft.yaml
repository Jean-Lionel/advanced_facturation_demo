models:
  CommissionDetail:
    compte_id : nullable id foreign:compte nullable
    client_id : nullable id foreign:clients
    order_id : nullable id foreign:orders
    user_id :  id foreign:users
    montant :  double
    activite: nullable string
    description: nullable text
    SoftDeletes

controllers:
  CommissionDetail:
    resource: web

