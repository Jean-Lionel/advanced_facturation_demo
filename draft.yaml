models:
  Activity:
    id: id
    name: string:255
    description: text nullable
    timestamps: true
    user_id: id foreign:users
    organisation_id: id foreign:organisations nullable
    relationships:
      belongsTo: User, Organisation

controllers:
  Activity:
    resource: web
